import { PermisoDisponiblePipe } from './permiso-disponible.pipe';

describe('PermisoDisponiblePipe', () => {
  it('create an instance', () => {
    const pipe = new PermisoDisponiblePipe();
    expect(pipe).toBeTruthy();
  });
});
